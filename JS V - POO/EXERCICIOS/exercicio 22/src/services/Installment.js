
class Installment {
  constructor(loanValue, numberOfInstallment, fees) {
    this.installmentValue = (loanValue*fees)/numberOfInstallment,
    this.installmentQuantities = numberOfInstallment
    this.status = false
  }

  o
}

module.exports = Installment